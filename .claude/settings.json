{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/verify.sh",
            "shell": "bash",
            "timeout": 90,
            "statusMessage": "verify.sh (tsc + lint:tokens + cargo check)"
          }
        ]
      }
    ]
  }
}
